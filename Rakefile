task default: [:execute, :test]

desc 'Execute mars rover program with the data from input.txt file'
task :execute do
  sh 'ruby src/mars_init.rb input.txt'
end

desc 'Run all test'
task :test do

end
